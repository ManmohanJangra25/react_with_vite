// import { useDispatch } from "react-redux";
import { createStore } from "redux";

const reducer = (store, action) => {
  return store;
};

const store = createStore(reducer);

console.log(store);

// const [store, action] = useDispatch({
//     type:
// })

export default store;
